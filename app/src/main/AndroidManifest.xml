<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.huy.thecoffeehouse">

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconcfhouse"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iconcfhouse"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheCoffeeHouse">
        <activity android:name=".DangNhap"></activity>
        <activity android:name=".TrangChu" />
        <activity android:name=".TinTucDetail" />
        <activity android:name=".Slide" />
        <activity android:name=".ManHinhLoading">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>